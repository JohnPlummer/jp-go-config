# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydb
DB_USER=postgres
DB_PASSWORD=secret

# Or use a connection URL (takes precedence)
# DATABASE_URL=postgres://postgres:secret@localhost:5432/mydb?sslmode=disable

# Server Configuration
SERVER_HOST=localhost
SERVER_PORT=8080
